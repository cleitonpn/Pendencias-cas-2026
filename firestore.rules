rules_version = '2';

// The app does not use Firebase Authentication — access is controlled by
// the app itself (admin PIN, producer PIN, stand PIN). All collections must
// therefore be readable and writable by any client.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
